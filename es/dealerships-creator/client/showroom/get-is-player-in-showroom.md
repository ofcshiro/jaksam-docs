---
title: "Saber si el jugador está en el showroom"
description: "Comprueba si el jugador local está actualmente en un showroom."
icon: "circle-question"
---

Devuelve `true` cuando el jugador está en un showroom, `false` cuando no lo está.

```lua Export
exports["dealerships_creator"]:isShowroomActive()
```
