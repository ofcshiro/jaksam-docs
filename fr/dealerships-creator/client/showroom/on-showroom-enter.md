---
title: "À l'entrée dans le showroom"
description: "Se déclenche côté client après que le joueur entre dans le showroom."
icon: "right-to-bracket"
---

Se déclenche après que le joueur entre dans le showroom.

```lua Event
RegisterNetEvent("dealerships_creator:onShowroomEnter", function()
    -- Tu peux ajouter tout ce qui doit se passer quand le joueur local entre dans le showroom
    -- Par exemple, masquer ton HUD
end)
```
