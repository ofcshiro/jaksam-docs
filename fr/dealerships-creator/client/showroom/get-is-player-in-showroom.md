---
title: "Savoir si le joueur est dans le showroom"
description: "Vérifie si le joueur local est actuellement dans un showroom."
icon: "circle-question"
---

Retourne `true` quand le joueur est dans un showroom, `false` sinon.

```lua Export
exports["dealerships_creator"]:isShowroomActive()
```
